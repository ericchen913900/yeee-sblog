---
import Layout from "../layouts/Layout.astro";
---

<Layout>
  <main>
    <div class="top">
      <div class="largeName aos">yeee</div>
      <h2 class="aos">AKA yeee, CTF Player</h2>
      <p class="aos">
        高中資安愛好者，Bamboo Fox 戰隊成員。熱愛資安與開發，平時撰寫 CTF
        write-up 與學習筆記，分享工具與解題思路。
      </p>
      <h2>Skills</h2>
      <img src="/avatar.jpg" alt="yeee profile photo" class="em" />
    </div>

    <div class="skill">
      <div>
        <h3>I do</h3>
        <ol>
          <li>CTF 競賽 </span></li>
          <li>Web Security</li>
          <li>Penetration Testing</li>
          <li>Reverse Engineering</li>
          <li>Cryptography</li>
          <li>Binary Exploitation</li>
          <li>Full Stack Development</li>
          <li>Write-up </li>
        </ol>
      </div>
      <div>
        <h3>Languages & Tools</h3>
        <ol>
          <li>Python</li>
          <li>C / C++</li>
          <li>Bash / Shell</li>
          <li>HTML / CSS</li>
        </ol>
      </div>
      <div>
        <h3>Security Tools</h3>
        <ol>
          <li>Burp Suite</li>
          <li>Wireshark</li>
          <li>Metasploit</li>
          <li>Nmap</li>
          <li>IDA Pro / Ghidra</li>
          <li>GDB / Pwndbg</li>
          <li>John the Ripper</li>
          <li>Hashcat</li>
        </ol>
      </div>
    </div>

    <h2>Education</h2>
    <div class="education">
      <div class="school aos">
        <div>
          <h3>東山高中</h3>
          <p>2021 - now</p>
        </div>
      </div>
    </div>

    <h2>Working Experiences</h2>
    <ul>
      <li>Bamboo Fox 戰隊 - CTF 隊員 <span>(2024 - Now)</span></li>
    </ul>

    <h2>Contest & Achievements</h2>
    <h3>2025</h3>
    <ul>
      <li>加入 Bamboo Fox 戰隊</li>
      <li>FINAL CTF 競賽 - 參與社群 CTF 競賽</li>

      <li>CTFtime 平台競賽 - 活躍參賽者</li> 
    </ul>

    <h2>Community & Conference</h2>
    <ul>
      <li>HITCON (台灣駭客年會) - 會眾參與 <span>(2025)</span></li>
      <li>SITCON 2025 - 學生計算機年會參與 <span>(2025)</span></li>
      <li>總統盃黑客松 - 參與國家級黑客松競賽 <span>(2025)</span></li>
    </ul>

    <div id="speaker">
      <h2>Statistics</h2>
      <div class="stats-container">
        <div class="stat-item">
          <div class="stat-number" id="blog-days">0</div>
          <div class="stat-label">BLOG DAYS</div>
        </div>
        <div class="stat-item">
          <div class="stat-number">3</div>
          <div class="stat-label">ARTICLES</div>
        </div>
        <div class="stat-item">
          <div class="stat-number">5+</div>
          <div class="stat-label">CTF EVENTS</div>
        </div>
        <div class="stat-item">
          <div class="stat-number">4</div>
          <div class="stat-label">SKILLS</div>
        </div>
      </div>
    </div>
  </main>

  <style>
    /* Edit-Mr 風格樣式，使用你的顏色和字型 */
    main {
      margin: auto;
      max-width: 1532px;
      width: 100%;
      padding: 25vh min(84px, 5vw) 0;
      line-height: 1.5;
      font-family: "Inter", sans-serif;
      color: #ffffff;
    }

    main .largeName {
      font-size: 3rem;
      font-weight: 700;
      letter-spacing: -0.5px;
      color: #ffffff;
    }

    main .largeName,
    .largeName + h2,
    h2 + p {
      opacity: 0;
      transition: 0.3s ease-out;
    }

    .largeName.ed,
    h2.ed,
    h2 + p.ed {
      opacity: 1;
    }

    main .largeName + h2 {
      transition-delay: 0.2s;
      color: #00ff88;
      font-family: "JetBrains Mono", monospace;
    }

    h2 + p {
      transition-delay: 0.4s;
    }

    main .largeName + h2 + p {
      font-size: 1.3rem;
      font-weight: 400;
      margin-bottom: 3rem;
      max-width: 800px;
      color: #cccccc;
    }

    p {
      margin-top: 1rem;
      color: #cccccc;
    }

    h2 {
      margin-top: 2rem;
      font-size: 1em;
      font-weight: 900;
      letter-spacing: 0.25rem;
      padding-bottom: 0.5rem;
      border-bottom: 1px solid #00ff88;
      text-transform: uppercase;
      color: #ffffff;
    }

    main .largeName + h2 {
      letter-spacing: normal;
      border: none;
      margin-bottom: 1rem;
      color: #00ff88;
    }

    .skill {
      display: flex;
      flex-wrap: wrap;
      padding: 1.3rem 0;
      position: relative;
      gap: 2rem;
    }

    .skill > div {
      flex: 1;
      line-height: 2;
    }

    .skill h3 {
      font-weight: 600;
      margin-bottom: 0.5rem;
      color: #00ff88;
    }

    .skill li {
      list-style-type: none;
      color: #e5e5e5;
    }

    .top {
      position: relative;
      z-index: 2;
    }

    .em {
      position: absolute;
      filter: grayscale(0.5) brightness(73%) contrast(192%);
      bottom: 1px;
      right: 0;
      z-index: -1;
      max-width: max(50vw, 30rem);
      height: 30rem;
      border-radius: 50%;
      object-fit: cover;
    }

    ul {
      margin: 1rem;
    }

    li {
      color: #e5e5e5;
      opacity: 0;
      transform: translateY(1rem);
    }

    li.ed {
      opacity: 1;
      transform: translateY(0);
      transition: 0.3s ease-out;
    }

    ul li {
      margin: 0.5rem;
    }

    main span {
      color: #00ff88;
      font-style: italic;
      font-family: "JetBrains Mono", monospace;
    }

    main a {
      text-decoration: none;
      position: relative;
      color: #00ff88;
    }

    main a::after {
      content: "";
      position: absolute;
      width: 100%;
      height: 2px;
      background-color: #00ff88;
      bottom: 0;
      left: 0;
      transform: scaleX(0);
      transition: transform 0.3s;
    }

    main a:hover::after {
      transform: scaleX(1);
    }

    .education {
      width: 100%;
      padding-block: 2rem;
      position: relative;
      margin-top: 2rem;
      margin-bottom: -1rem;
    }

    .education::before {
      position: absolute;
      top: 0;
      left: 50%;
      transform: translateX(-50%);
      content: "";
      display: block;
      width: 1px;
      height: 100%;
      background: linear-gradient(
        to bottom,
        #000,
        #00ff88 10%,
        #00ff88 80%,
        #000
      );
      margin: 1rem 0;
    }

    .education .school:nth-child(odd) {
      padding-right: calc(50% + 2rem);
      text-align: right;
      transform: translateX(2rem);
    }

    .education .school:nth-child(even) {
      padding-left: calc(50% + 2rem);
      transform: translateX(-2rem);
    }

    .school {
      position: relative;
      opacity: 0;
      transition: 0.5s ease-out;
    }

    .education .school.ed {
      opacity: 1;
      transform: none;
    }

    .school::after {
      content: "";
      display: block;
      width: 1.5rem;
      height: 1px;
      position: absolute;
      top: 50%;
      left: 50%;
      transform: translate(-100%, -50%);
      opacity: 0;
    }

    .school:nth-child(odd)::after {
      background: linear-gradient(90deg, #000, #00ff88);
    }

    .school:nth-child(even)::after {
      background: linear-gradient(270deg, #000, #00ff88);
      transform: translate(0%, -50%);
    }

    .school p {
      margin-top: 0;
      color: #00ff88;
      font-family: "JetBrains Mono", monospace;
    }

    .school h3 {
      color: #ffffff;
    }

    .stats-container {
      display: flex;
      justify-content: center;
      gap: 3rem;
      flex-wrap: wrap;
      margin: 2rem 0;
    }

    .stat-item {
      text-align: center;
      padding: 1.5rem;
      background: rgba(0, 255, 136, 0.1);
      border: 1px solid #00ff88;
      border-radius: 8px;
      min-width: 150px;
      transition: all 0.3s ease;
    }

    .stat-item:hover {
      background: rgba(0, 255, 136, 0.2);
      transform: translateY(-4px);
    }

    .stat-number {
      font-size: 2.5rem;
      font-weight: 800;
      color: #00ff88;
      margin-bottom: 0.5rem;
      font-family: "JetBrains Mono", monospace;
    }

    .stat-label {
      font-size: 0.8rem;
      color: #cccccc;
      text-transform: uppercase;
      letter-spacing: 0.1em;
      font-family: "JetBrains Mono", monospace;
    }

    h3 {
      color: #00ff88;
      font-size: 1.2rem;
      margin-top: 2rem;
      margin-bottom: 1rem;
    }

    /* 響應式設計 */
    @media (max-width: 50rem) {
      .em {
        opacity: 0.6;
      }

      .stats-container {
        flex-direction: column;
        align-items: center;
        gap: 1rem;
      }

      .stat-item {
        width: 100%;
        max-width: 300px;
      }
    }

    h1 {
      display: none;
    }
  </style>
</Layout>

<script>
  // Edit-Mr 風格的動畫效果
  function isElementInViewport(el) {
    const rect = el.getBoundingClientRect();
    return rect.bottom < 0 || rect.top > window.innerHeight;
  }

  function addClassToVisibleElements() {
    var aosElements = document.querySelectorAll(".aos, li");
    aosElements.forEach(function (aosElement) {
      if (!isElementInViewport(aosElement)) aosElement.classList.add("ed");
      else aosElement.classList.remove("ed");
    });
  }

  const showBody = () => {
    document.body.style.opacity = 1;
    setTimeout(() => {
      addClassToVisibleElements();
    }, 300);
  };

  // 計算部落格運行天數
  function updateBlogDays() {
    const startDate = new Date("2024-01-01");
    const currentDate = new Date();
    const timeDiff = currentDate.getTime() - startDate.getTime();
    const daysDiff = Math.ceil(timeDiff / (1000 * 3600 * 24));

    const blogDaysElement = document.getElementById("blog-days");
    if (blogDaysElement) {
      let currentNumber = 0;
      const increment = Math.ceil(daysDiff / 50);
      const timer = setInterval(() => {
        currentNumber += increment;
        if (currentNumber >= daysDiff) {
          currentNumber = daysDiff;
          clearInterval(timer);
        }
        blogDaysElement.textContent = currentNumber.toString();
      }, 30);
    }
  }

  // 頁面載入和滾動事件
  document.addEventListener("scroll", addClassToVisibleElements);
  window.addEventListener("pageshow", showBody);

  document.addEventListener("DOMContentLoaded", () => {
    console.log("About page loaded successfully!");
    updateBlogDays();

    // 初始動畫
    setTimeout(() => {
      addClassToVisibleElements();
    }, 100);
  });
</script>
